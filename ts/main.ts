import * as qd from 'QuickDOM.js' ;
import * as dt from 'dat.js' ;

var t: HTMLElement = qd.Id("term") ;
var m: HTMLElement = qd.Id("main") ;
var p: HTMLElement = qd.Id("ps") ;

function initControls(): void {
}

function initDefault(): void {
}
